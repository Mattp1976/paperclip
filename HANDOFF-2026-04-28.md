# Handoff — 2026-04-28, end of day

## Where we are

### What's solid
- **Anthropic billing** — credits added, new key on local `.env` and Railway.
- **Railway deploy pipeline** — was silently failing all day; now fixed. Two real bugs found and shipped:
  - Healthcheck path was `/health`, server only serves `/api/health` (commit `75a504db`).
  - `PAPERCLIP_DEPLOYMENT_EXPOSURE` was `private`, blocking Railway's healthcheck via the privateHostnameGuard. Set to `public` on the service.
- **Sprint 5 (PMP S5)** — decision queue + cost-as-confidence shipped (commit `e47117b9`), and is now actually live on prod for the first time today.
- **Brand** — `marketing/BRAND.md` Score Editorial philosophy, logo (mark + lockup) in `marketing/logo/`, IBM Plex Serif typography.
- **Landing page** — at `https://www.orqestra.run/welcome/`. Conversion-funnel layout: hero → live preview via `/api/preview` → simple flow → example companies → trust → CTA.
- **`/api/preview` endpoint** — public, unauthenticated, calls Claude Haiku for a generated plan in ~1.5–3s. Falls back to a deterministic template if the LLM call fails.
- **Local dev** — server running on `:3100`, `local_trusted` mode, agents on `claude_local`.

### What's unverified or broken
- **End-to-end product run on prod has never been tested today.** We don't know if the 39 agents on Agent Swarm can actually execute an outcome. They're presumably on `claude_local`, which needs Claude Code CLI installed and authenticated inside the Railway container. The Dockerfile globally installs `@anthropic-ai/claude-code@latest` (line 40), but we never verified it runs end-to-end with the new Anthropic key.
- **Future Collective is NOT on prod.** The landing page's "113-agent company already live" claim is technically wrong — prod has one company called "Agent Swarm" with 39 agents. Future Collective lives only in local dev.
- **better-auth password sign-in via API is failing on prod for `matt@future-collective.co.uk`.** I reset the password directly in the DB (hash format `salt:hash`, verified locally with `verifyPassword` from `better-auth/crypto`), but prod's `/api/auth/sign-in/email` rejects it as `INVALID_EMAIL_OR_PASSWORD`. Some crypto or version mismatch between local node_modules and the deployed image. Sign-up still works (`claude-test@orqestra.run` was created successfully).
- **`instance_admin` doesn't grant cross-company access.** The API's company-scoped routes check `company_memberships` directly, not the global role.
- **Marketing → product handoff is not wired.** Clicking "Run this company" on the landing stores the outcome in sessionStorage and redirects to `/sign-in?continue=/start&outcome=...`, but the React `/start` page doesn't read either. The wow-moment evaporates after sign-up.

## CRITICAL — heartbeat-timer credit burn

**Local server has been silently burning ~$87/day in Anthropic credits, with no work being done.** Discovered late evening.

### What's happening
The heartbeat scheduler auto-pokes idle agents on a recurring per-agent cadence (`source=timer, reason=heartbeat_timer, requested_by=heartbeat_scheduler`). 316 timer-triggered runs in the last 24h, all on Future Collective. No routines, no user requests, no outcomes — pure scheduler self-fire.

### Today's distribution

| agent              | runs | spent  | cadence    |
| ------------------ | ---- | ------ | ---------- |
| Founding Engineer  |  148 | $37.77 | ~ 10 min   |
| CTO                |  102 | $24.73 | ~ 14 min   |
| CMO                |   24 | $10.61 | ~ hourly   |
| CFO                |   23 |  $6.46 | ~ hourly   |
| CEO                |   22 |  $7.48 | ~ hourly   |
| Research Director  |    2 |  $0.28 | manual     |

### Action required tomorrow (do this BEFORE restarting the server)

1. Find the heartbeat scheduler code — likely `server/src/services/heartbeat.ts` or similar.
2. Identify the per-agent timer cadence rule. The Founding Engineer / CTO firing every 10–14 min suggests a per-role default, not a config.
3. Either: gate the timer behind "only fire if there's actual work in the queue", OR set the default to a much longer interval (24h) and require explicit per-agent overrides.
4. Until that's fixed: **do not restart the local server**. The temp script `/tmp/start-orqestra-local.sh` will start it back up with the burn loop intact.

### Diagnostic SQL to confirm fix later

```sql
-- After restart, check timer-triggered runs aren't piling up:
SELECT date_trunc('hour', created_at) hr, count(*)
FROM agent_wakeup_requests
WHERE reason='heartbeat_timer' AND created_at > NOW() - INTERVAL '4 hours'
GROUP BY 1 ORDER BY 1 DESC;
```

The diag script is at `server/_diag_burnrate.mjs`. Delete it once the underlying bug is fixed.

---

## What to do tomorrow, in order

1. **Verify the product actually runs on prod.** Sign in via the browser as `matt@future-collective.co.uk` using whatever password worked before today (not the temp one I set — that's broken at the API layer). If the browser-form sign-in works, navigate to Agent Swarm and trigger one heartbeat run on any agent. Watch for `succeeded` vs `Credit balance too low` vs `claude code not found` etc. This is the single most important diagnostic before doing anything else.
2. **If runs work:** wire the marketing → product handoff (read outcome from URL or sessionStorage on `/start`).
3. **If runs fail:** debug the actual failure — most likely Claude Code CLI auth in the container, or some `claude_local` adapter expectation that prod doesn't satisfy.
4. **Either way, fix the landing claim.** Either import Future Collective's templates onto prod so the 113-agent claim becomes true, or rewrite that line to match reality (e.g. "agents already running real outcomes").

## What to leave alone

- The `/welcome` URL hasn't been shared with anyone yet — no rush to fix the claim.
- The brand and landing layout are fine as design objects; the issue is the truth of the copy, not the form.
- Don't redeploy more landing changes until step 1 above has been done.

## Tasks to clean up

- `#92` (openrouter-local adapter Phase A) — built, then reverted in same session. Mark completed-rolled-back.
- `#62` (Railway healthcheck path mismatch) — fixed today, mark completed.
- `#88` (Install ANTHROPIC_API_KEY on Railway prod) — was already marked completed but Railway didn't actually have it set when checked today. Now genuinely set with the new key.

## Useful state

- Prod project: `courageous-luck` (`0333545a-9abe-418e-80e9-4dac03f2d15b`)
- Prod Postgres public URL: `postgresql://postgres:WyZimTXfhmIjoyJMDdGQVyyXNchcZRFM@trolley.proxy.rlwy.net:38197/railway`
- Railway CLI installed at `~/.local/bin/railway` (Rust v4.43.0, authenticated as matt@future-collective.co.uk)
- Test user on prod: `claude-test@orqestra.run` / `ClaudeTest2026!` (instance_admin, not a member of any company)
- Local landing renders cleanly via `--headless` Chrome — preview pipeline works for screenshots if needed.
